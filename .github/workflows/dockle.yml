---
name: Dockle

on:
  push:
    branches:
      - master
      - staging
  pull_request:

jobs:
  dockle:
    runs-on: ubuntu-latest

    env:
      DOCKER_CONTENT_TRUST: 1
      DOCKLE_VERSION: 0.4.15

    steps:
      - uses: actions/checkout@v6.0.2

      - name: Download and install dockle v${{ env.DOCKLE_VERSION }}
        run: |
          set -eux
          curl -L -o dockle.deb "https://github.com/goodwithtech/dockle/releases/download/v${DOCKLE_VERSION}/dockle_${DOCKLE_VERSION}_Linux-64bit.deb"
          sudo dpkg -i dockle.deb

      - run: |
          cp .config/docker_example.env .config/docker.env
          cp ./compose_example.yml ./compose.yml

      - name: Build web image (docker build)
        run: |
          set -eux
            docker compose up -d web
          docker build -t "yamisskey-web:ci" .
          docker image ls

      - name: Mount tmpfs for Dockle tar
        env:
          TMPFS_SIZE: 8G
        run: |
          set -eux
          sudo mkdir -p /mnt/dockle-tmp
          sudo mount -t tmpfs -o size=${{ env.TMPFS_SIZE }} tmpfs /mnt/dockle-tmp
          free -h
          df -h

      - name: Save image tar into tmpfs
        run: |
          set -eux
          docker save yamisskey-web:ci -o /mnt/dockle-tmp/yamisskey-web.tar
          ls -lh /mnt/dockle-tmp/yamisskey-web.tar

      - name: Run Dockle Scan (tar input)
        run: |
          set -eux
          dockle --exit-code 1 --input /mnt/dockle-tmp/yamisskey-web.tar
