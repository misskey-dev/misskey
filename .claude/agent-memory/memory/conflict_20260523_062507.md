# issue16270 merge conflict: UserSuspendService

- Branch: issue16270
- Merge source: upstream/develop
- Conflict file: packages/backend/src/core/UserSuspendService.ts
- Merge base: 76beac41d23ce4a07bc267eb0e7685f472f1fe48

## Intent

HEAD changes replaced suspend-time unfollow jobs with `isFollowerSuspended` updates so following rows survive suspension and can be restored on unsuspend.

upstream/develop kept the older `unFollowAll` behavior but updated style around ignored catch arguments and TypeORM `select` object syntax.

## Resolution

Kept HEAD's `suspendFollowings` / `restoreFollowings` behavior because it is the PR's core behavior. Adopted upstream's ignored catch argument style (`catch(_ => {})`). No functions were dropped other than upstream's `unFollowAll`, which conflicts with the PR's explicit behavior.
