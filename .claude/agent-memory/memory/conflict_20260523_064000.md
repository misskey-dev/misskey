# ap-susp-update merge conflict: UserSuspendService

- Branch: ap-susp-update
- Merge source: issue16270
- Conflict files:
  - packages/backend/src/core/UserSuspendService.ts
  - packages/backend/test/unit/UserSuspendService.ts
- Merge base: d72119fd080c5438cdd668f4d0f86b955f629971

## Intent

HEAD adds `ApDeliverManagerService` and uses `addAllKnowingSharedInboxRecipe` plus `addFollowersRecipe` when broadcasting suspend and unsuspend activities.

issue16270 was already updated to current develop and preserves the `isFollowerSuspended` suspend/restore behavior. It also converts the stale Jest unit test to Vitest.

## Resolution

Kept the `ApDeliverManagerService` delivery path from HEAD because it is the core purpose of ap-susp-update. Kept issue16270's `suspendFollowings` and `restoreFollowings` behavior. In the test, kept the extra providers required by `ApDeliverManagerService` and converted all mocks to `vi.fn`.
