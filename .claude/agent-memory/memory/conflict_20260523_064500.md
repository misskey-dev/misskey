# remote-suspend merge conflict: UserSuspendService

- Branch: remote-suspend
- Merge source: ap-susp-update
- Conflict files:
  - packages/backend/src/core/UserSuspendService.ts
  - packages/backend/test/unit/UserSuspendService.ts
- Merge base: 97f4c6a2e62e9c3b10840e8518ef936eb1e37373

## Intent

HEAD changes suspend delivery from Delete and Undo Delete activities to AccountUpdateService Update Person delivery, and adds remote-side suspension state through `isRemoteSuspended`.

ap-susp-update had already been updated to current develop, preserves `isFollowerSuspended` follow restoration, and converts the unit tests to Vitest.

## Resolution

Kept HEAD's AccountUpdateService-based Update Person delivery and remote suspend/unsuspend entrypoints. Kept ap-susp-update's updated follow restoration and Vitest mock style. The AccountUpdateService path keeps the shared-inbox delivery manager recipe through `publishToFollowersAndSharedInboxAndRelays`.
