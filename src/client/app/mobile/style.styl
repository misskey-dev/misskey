@import "../app"
@import "../reset"

#wait
	top auto
	bottom 15px
	left 15px

html
	height 100%
	background #ececed

	&[data-darkmode]
		background #191B22

body
	display flex
	flex-direction column
	min-height 100%
